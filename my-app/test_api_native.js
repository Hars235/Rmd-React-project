const https = require('https'); const data = JSON.stringify({city: 'Bengaluru', search_term: 'Jayanagar'}); const options = { hostname: 'reachmydoctor.in', path: '/api/v1/autocomplete/get_localities', method: 'POST', headers: { 'Content-Type': 'text/plain', 'Content-Length': data.length } }; const req = https.request(options, res => { let body = ''; res.on('data', d => body += d); res.on('end', () => console.log(body)); }); req.on('error', error => console.error(error)); req.write(data); req.end();
